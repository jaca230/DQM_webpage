const SettingTypes = Object.freeze({
  NUMBER: 'number',
  STRING: 'string',
  INT: 'int',
  BOOLEAN: 'boolean',
  ARRAY: 'array',
  OBJECT: 'object',
  COLOR: 'color',
});

export default SettingTypes;
